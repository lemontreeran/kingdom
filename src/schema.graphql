# import User from './generated/prisma.graphql'

type Transactions {
     id: ID!
     memo: String
}

type Query {
     user(username: String!): User
}

type Mutation {
     signup(username: String!): User!
     payment(amount: String!, senderName String!, recepientName: String!, memo: String)：Transaction
}
